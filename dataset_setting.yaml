# The path to the directory that stores the offline data
data_dir: dataset
# The name of dataset that contains the required fields
development_build_city: ["image_data.map_tab", "entity_type", "action_name", "step_reward", "mini_goal", "mini_score", "game_id"]
